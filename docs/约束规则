1.EFFormView 的formTitleNames属性传入的参数为一个字符串，该字符串必须是采用英文逗号","分割的字符串。
<br>字符串可以转换成一个数组，数组的长度正好是表格的宽度.

2.设置表格的列数和行数时时要包含表格的表头的。

3.isInput参数设置为true的话标识该表格的每一个单元格都是输入框;inputRows参数以"2,3"的形式设定某些行的类型为输入框。
当设置有inputRows参数时,isInput 参数的设置将会变成无效的。